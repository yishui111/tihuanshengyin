# 部署方案 · 复原/核对速查（tihuanshengyin）

> 目标：在新电脑上复原本项目。本仓库不含引擎/权重/运行时，
> 先按 `DEPLOY.md` 逐节装好，再对本文件核对结构与代码约定。
> **每次优化/修复功能后必须同步更新本文件（代码约定、目录、命令、验证方法）。**

## 1. 项目是什么

Windows 离线语音克隆/换声服务集合，全部本地运行、无需联网：

| 功能 | 说明 | 端口 | 启动方式 |
|---|---|---|---|
| 工作台 | 统一页面：文件夹批量说话人换声、单文件换声、输出浏览 | 8000 | `start.bat`（默认必启） |
| A | RVC 换音色（训练工程发布的角色放这） | 8010 | `start.bat`（默认启） |
| B | OpenVoice 任意人声克隆（素材+参考人声→参考音色） | 8020 | `start.bat B` |
| C | SoVITS 中配角色换声（角色模型在 `sovits_service\models\`） | 8030 | `start.bat C` |
| D | GPT-SoVITS ASR+重合成（"重新说一遍"，不用于替换） | 8040 | `start.bat D` |

启停只有 2 个脚本：`start.bat` 默认只启动 工作台+功能A（日常换声够用）；
需要其他引擎显式传参，如 `start.bat A C`、`start.bat B`、`start.bat A B C D`（全开）；
`start.bat stop` 停止全部、`start.bat stop B` 只停 B；`start.bat help` 看用法。
`stop.bat` 停止全部。

## 2. 新电脑要求

- Windows 10/11 64 位；建议英文路径（如 `D:\huan-sheng`）。
- NVIDIA 显卡（驱动 ≥ 522）：功能C/D 推理走 CUDA，**必须有**；功能B 与工作台无卡也能跑（慢）。
- 磁盘剩余 ≥ 40GB（Python 环境 + 引擎 + 权重约 25~35GB）。
- 杀毒软件需放行 `runtime\pyXXX\python.exe`（否则窗口一闪而过）。

## 3. 目录结构（复原/核对清单）

```
<仓库根>\
├─ hub\                         换声工作台（8000）
│  ├─ server.py                 FastAPI 主服务 + 批量任务编排
│  ├─ diarize.py                VAD + ECAPA 声纹聚类 → 说话人检测/分段
│  ├─ pipeline.py               批量管线：分离→逐段换声→拼接→视频混流
│  ├─ roles.py                  角色注册表（合并 A/C/D）
│  └─ web\index.html            页面（批量/单文件/输出浏览）
├─ openvoice_service\           功能B 服务 + checkpoints_v2\（V2 权重）
│  └─ openvoice_clone_api.py    FastAPI，端口 8020
├─ rvc_service\                 功能A 服务（rvc_character_api.py，8010）
│  └─ pymss_models\             人声分离模型（工作台批量分离用）
├─ rvc\                         RVC-WebUI 引擎（克隆，见 DEPLOY.md）
│  └─ assets\{hubert_base,rmvpe,weights,indices}
├─ sovits_service\              功能C 服务
│  ├─ so-vits-svc-4.1-Stable\   引擎源码（4.1-Stable，含 pretrain\）
│  ├─ models\                   角色模型（json+pth+index，自备）
│  └─ sovits_cn_api.py          FastAPI，端口 8030
├─ gptsovits_service\           功能D 服务
│  ├─ GPT-SoVITS\               引擎源码（克隆）
│  ├─ asr\SenseVoiceSmall\      中文 ASR 模型
│  ├─ models\<角色>\            角色权重（ckpt+pth+ref.wav+ref_text.txt，自备）
│  └─ gptsovits_cn_api.py       FastAPI，端口 8040
├─ runtime\                     Python 运行时 + ffmpeg + 模型缓存
│  ├─ py310\                    功能B 用的 Python 3.10 环境
│  ├─ py312\                    工作台 + 功能A/C/D 用的 Python 3.12 环境
│  ├─ ffmpeg\bin\               ffmpeg / ffprobe（取音轨、混流）
│  └─ cache\                    声纹模型/transformers/torch 离线缓存
├─ ziliao\                      素材/输入；ziliao\输出\ 正式输出（代码自动建）
├─ tests\                       原机自测素材/脚本（个人素材，不入库）
├─ README.md  DEPLOY.md         使用说明 / 部署手册
├─ 部署方案.md                  本文件
├─ AGENTS.md                    项目约定（AI/协作工作规则）
├─ start.bat / stop.bat         唯一启停脚本（start.bat 支持参数启停）
```

## 4. 依赖（引擎与运行时的装法见 DEPLOY.md）

- 功能B：py310 环境（OpenVoice V2、torch 2.1.2+cu118、librosa…）
- 工作台 + A/C/D：py312 环境（torch cu118、speechbrain、scikit-learn、pymss、
  funasr、onnxruntime-gpu==1.17.1、fastapi、uvicorn…）
- PATH 需含 `runtime\ffmpeg\bin`（start.bat 已配）

## 5. 启动与自检

```bat
start.bat          :: 启动工作台 + 功能A
start.bat A B C D  :: 全开
stop.bat           :: 停止全部
```

自检：

```powershell
curl http://127.0.0.1:8000/api/health          # 工作台
curl http://127.0.0.1:8010/health              # A
curl http://127.0.0.1:8030/health              # C
curl -X POST -F "audio=@<测试音频>.wav" -F "character=<角色>" http://127.0.0.1:8030/convert -o out.wav
```

判定：接口返回 200；输出时长 ≈ 素材时长（内容保留）；听感音色向角色靠拢。

## 6. 关键代码约定（复原/改动时必须保留，曾踩坑）

1. 功能B：素材写 `input.wav`、参考写 `ref.wav`，**禁止共用同一个文件名**
   （曾因共用 `normalized.wav` 导致素材被参考覆盖）。
2. 功能B：音色向量用 `extract_se_robust()`（能量 VAD 分段 + 多段平均），
   不用会联网的 silero VAD；默认 `tau=0.15`。
3. **说话人检测**（hub/diarize.py）：能量 VAD → ECAPA 声纹嵌入（离线，
   speechbrain `spkrec-ecapa-voxceleb`，缓存 `runtime\cache\`）→ 余弦距离层次聚类。
   少数说话人需 ≥2 段或占比 ≥25%，防止把 1 句口误当第二人。
4. **逐段换声**（hub/pipeline.py）：按说话人标签把语音段送引擎 `/convert`，
   再按原时间轴拼回（段边界 45ms 淡入淡出防爆音）；视频用
   ffmpeg `-c:v copy -c:a aac` 混流，画面不重编码。
   **换声粒度（重要，避免"一句话一段"的生硬感）**：
   单说话人 → 整段一次转换（引擎内部自动切片）；多说话人 → 先合并相邻
   同人段（间隔 ≤1.5s）再转换，每段尽量长，保证段内变调一致、句间连贯。
5. 批量管线调用 A/C 的 `/convert` 只传 `character` + `auto_pitch=true`，
   其余参数用服务默认值；D（重合成）不用于批量逐段模式（时长不保）。
6. hub 的 C 角色名要换算：模型文件名（`nahida41_G_*.pth`/`randenEi_G_*.pth`）≠
   接口角色名（`nahida`/`raiden`），见 `hub/roles.py` 的 `C_CHAR_OVERRIDES`。
7. hub 以脚本方式运行（`python hub/server.py`）：嵌入版 Python 不会自动把
   脚本目录加入 sys.path，`server.py` 开头必须 `sys.path.insert(0, 脚本目录)`；
   各引擎封装靠 `os.chdir(引擎根)` + `sys.path.insert` 导入引擎模块。
8. 引擎目录全部可用环境变量覆盖：`RVC_ROOT`/`SOVITS_SRC`/`GSV_ROOT`/`OV_CKPT`/
   `TMP_ROOT`/`API_PORT`/`HUB_PORT`，换目录不用改代码。

## 7. 已知问题

- 首次启动加载模型 10~30 秒属正常；SoVITS 首个角色约 15~25 秒，之后秒出。
- **onnxruntime 必须用 1.17.1（CUDA 11.8 版）**：1.18+ 需要 CUDA 12 dll，
  本工程只有 cu118，会导致 onnx 回退 CPU、SoVITS 首次加载 80 秒+（像卡死）。
  已装 `onnxruntime-gpu==1.17.1` + `nvidia-cudnn-cu11`（cudnn64_8.dll），
  `sovits_cn_api.py` 启动时会自动把 nvidia/torch 的 dll 目录加入 PATH。
- 两人重叠说话无法完美分离（段会归给其中一人）。
- 功能D 会"重新说一遍"句子，保留原语气节奏请用功能C。
- 端口被占用：`set API_PORT=XXXX` / `set HUB_PORT=XXXX` 后启动。
- 无 NVIDIA 显卡：B 与工作台可 CPU（慢）；C/D 需 CUDA。

## 8. 更新约定

- 每次优化/修复功能后，必须同步更新本文件（代码约定、目录、命令、验证方法）。
- 本文件与 `DEPLOY.md` 是"换电脑复原项目"的依据，不允许过时。
